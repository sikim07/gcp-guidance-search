# 브루트포스 vs ANN 벤치마크

측정일: 2026-09-15
임베딩 차원: 1536 (`text-embedding-3-small`)
실제 코퍼스 청크 수(표시용): **45**
pgvector 전환 조건: 청크 **5000**개 초과 또는 유사도 계산 **500ms** 초과.

| 청크 수 | 브루트포스 cosine (ms) | IVF/HNSW 계열 근사 (ms) | 500ms 임계값 |
| ---: | ---: | ---: | --- |
| 100 | 0.23 | 0.10 | 이하 ← 현재 규모에 가장 가까운 구간 |
| 1,000 | 2.55 | 0.58 | 이하 |
| 10,000 | 22.96 | 1.23 | 이하 |
| 100,000 | 241.86 | 10.52 | 이하 |

```
브루트포스 상대 막대 (최댓값 대비)
    100 █ 0.2ms
   1000 █ 2.6ms
  10000 ██ 23.0ms
 100000 ████████████████████ 241.9ms
현재 청크 ≈ 45  (그래프에서 100 행을 현재 규모 근사치로 표시)
```

## 결론

현재 예상 청크 수는 수백 개다. 이 구간에서 브루트포스는 0.23ms (100개) 수준이며 500ms를 넘지 않는다.
벡터DB/pgvector는 쓰지 않는다. 구현은 `lib/retrieval/cosine.ts` 의 브루트포스 함수다.

ANN 열은 pgvector HNSW와 같은 복잡도 계열의 in-process IVF 스캔이다. `DATABASE_URL` 에 pgvector가 있으면 같은 스크립트에 SQL 측정을 추가하면 된다.
