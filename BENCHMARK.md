# 브루트포스 vs ANN 벤치마크

측정일: 2026-09-17
임베딩 차원: 1536 (`text-embedding-3-small`)
실제 코퍼스 청크 수(표시용): **721**
pgvector 전환 조건: 청크 **5000**개 초과 또는 유사도 계산 **500ms** 초과.

| 청크 수 | 브루트포스 cosine (ms) | IVF/HNSW 계열 근사 (ms) | 500ms 임계값 |
| ---: | ---: | ---: | --- |
| 100 | 0.21 | 0.08 | 이하 |
| 1,000 | 1.97 | 0.24 | 이하 ← 현재 규모에 가장 가까운 구간 |
| 10,000 | 19.62 | 1.03 | 이하 |
| 100,000 | 201.33 | 9.67 | 이하 |

```
브루트포스 상대 막대 (최댓값 대비)
    100 █ 0.2ms
   1000 █ 2.0ms
  10000 ██ 19.6ms
 100000 ████████████████████ 201.3ms
현재 청크 ≈ 721  (그래프에서 1,000 행을 현재 규모 근사치로 표시)
```

## 결론

현재 예상 청크 수는 수백 개다. 이 구간에서 브루트포스는 0.21ms (100개) 수준이며 500ms를 넘지 않는다.
벡터DB/pgvector는 쓰지 않는다. 구현은 `lib/retrieval/cosine.ts` 의 브루트포스 함수다.

ANN 열은 pgvector HNSW와 같은 복잡도 계열의 in-process IVF 스캔이다. `DATABASE_URL` 에 pgvector가 있으면 같은 스크립트에 SQL 측정을 추가하면 된다.
