# GCP 가이드라인 검색기 — 포트폴리오 / 이력서

이력서 한 줄, 자기소개서 STAR, 면접 숫자 근거를 같은 문서에 둔다. 운영 지표는 배포 후 2–3주 로그로 채운다.

---

## 프로젝트명

GCP 가이드라인 검색기

## 한 줄 요약

FDA·ICH·식약처 등 여러 기관에 흩어진 임상시험 규제 가이드라인의 개정을 자동으로 감지·반영하고, 이를 자연어로 검색할 수 있게 만든 도구.

## 이력서용 한 줄 (선택)

임상시험 GCP 가이드라인의 개정을 FDA·식약처·법령 사이트에서 매일 감지하고, 조항 단위 근거와 함께 자연어로 조회하는 Next.js 파이프라인. 청크 45개 규모에서 브루트포스 코사인 0.23ms를 실측해 벡터DB 없이 설계.

---

## 필요 배경 (Situation)

EDC(임상시험 데이터 처리 시스템) 프론트엔드 개발자로 일하면서, 필드 검증 로직이나 감사추적(Audit Trail) 요건을 설계할 때마다 "이게 GCP나 식약처 규정상 근거가 뭐지?"를 확인해야 하는 상황이 자주 있었다. 그런데 임상시험 실무자가 아니다 보니 관련 규정을 처음부터 찾아야 했고, 규정 자체가 FDA·ICH·식약처 등 여러 사이트에 흩어져 있으며 주기적으로 개정되는데, 그 변화를 추적하는 게 항상 수작업이었다.

## 시나리오

- **본인 사용:** 신규 필드의 검증 로직을 짜야 할 때, 관련 규정을 자연어로 질문하면 근거 조항과 출처를 즉시 확인
- **외부 실사용자 (CRA/RA):** 사이트 모니터링 중 특정 규정(예: 이탈 보고 기한)이 헷갈릴 때, 커뮤니티에서 발견한 이 도구로 빠르게 확인

---

## 의사결정 근거 (Action)

### 왜 벡터DB를 안 썼는가

청크 수가 수백 개(현재 시드 **45개**) 수준인 규모에서 브루트포스 코사인 유사도와 ANN(pgvector HNSW와 같은 복잡도 계열의 in-process IVF)을 같은 1536차원 데이터로 실측했다.

| 청크 수 | 브루트포스 (ms) | ANN 근사 (ms) | 500ms 임계값 |
| ---: | ---: | ---: | --- |
| 100 (현재 규모 근사) | **0.23** | 0.10 | 이하 |
| 1,000 | 2.55 | 0.58 | 이하 |
| 10,000 | 22.96 | 1.23 | 이하 |
| 100,000 | 241.86 | 10.52 | 이하 |

현재 구간은 0.23ms이고 100,000개에서도 500ms를 넘지 않는다. 청크 **5,000개** 또는 유사도 계산 **500ms**를 넘으면 pgvector 전환을 검토한다는 조건을 코드 주석·README·`BENCHMARK.md`에 명시해, 근거 있는 최소 구성으로 설계했다.

### 왜 개정 자동 감지를 핵심으로 뒀는가

검색 자체보다, "규정이 바뀌었는지 사람이 매번 확인해야 하는 문제"가 더 근본적인 비효율이라 판단해 파이프라인의 중심으로 설계했다.

- 1차: FDA Date Issued / 식약처 고시일이 저장된 값보다 최신이면 개정
- 2차: 날짜가 같아도 PDF SHA-256이 다르면 개정
- 바뀐 조항만 재청크·재임베딩 (전체 재처리 금지)
- Vercel Cron 매일 1회, Slack webhook 또는 로그로 알림

### 왜 Claude Code Stop 훅으로 자동 코드 리뷰를 넣었는가

AI 에이전트가 구현한 코드를 사람이 매번 검토하는 대신, 다른 모델(Gemini 또는 GPT)이 `git diff`를 구조적으로 검증하게 만들어 신뢰도를 높이는 개발 프로세스 자체를 설계했다. 심각하면 `{ "decision": "block" }`으로 종료를 막고, 결과는 `.claude/review-log.jsonl`에 남긴다.

---

## 측정 가능한 결과 (Result)

| 지표 | 현재 값 | 메모 |
| --- | --- | --- |
| 브루트포스 vs ANN | 45청크 규모 근사 **0.23ms** / 100k **241.86ms** | 기술 결정의 정량 근거. 상세는 `BENCHMARK.md` |
| pgvector 전환선 | 5,000 청크 또는 500ms | 아직 미도달 |
| 개정 감지 주기 | 1일 | Vercel Cron 03:00 UTC |
| 시드 코퍼스 | 문서 8 / 청크 45 | E6(R2), Part 11, eSource, RBM, Informed Consent, 식약처 ICH GCP, KGCP 별표 4 |
| 검색 쿼리 수 | _배포 후 2–3주_ | `search_logs` |
| 재방문율 | _배포 후 2–3주_ | IP 해시 |
| 👍 비율 | _배포 후 2–3주_ | `feedback` |
| 실제 감지 개정 건수 | _운영 후_ | `change_log` (시드는 초기 적재로 기록됨) |
| 규정 확인 소요시간 | _본인 실측_ | 기존 검색엔진 대비 단축폭 |

---

## 기술 스택

Next.js (App Router, TypeScript strict) · Vercel Cron · Supabase Postgres (벡터 컬럼 없음) · OpenAI `text-embedding-3-small` · Claude Haiku · Vitest · Playwright · Claude Code Stop 훅

Claude Code 재구현용 원문은 `docs/IMPLEMENTATION_PROMPTS.md`.
