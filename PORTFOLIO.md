# GCP 가이드라인 검색기 — 포트폴리오 정리

## 프로젝트명

GCP 가이드라인 검색기

## 한 줄 요약

FDA·ICH·식약처 등 여러 기관에 흩어진 임상시험 규제 가이드라인의 개정을 자동으로 감지·반영하고, 이를 자연어로 검색할 수 있게 만든 도구.

## 필요 배경 (Situation)

EDC(임상시험 데이터 처리 시스템) 프론트엔드 개발자로 일하면서, 필드 검증 로직이나 감사추적(Audit Trail) 요건을 설계할 때마다 "이게 GCP나 식약처 규정상 근거가 뭐지?"를 확인해야 하는 상황이 자주 있었다. 임상시험 실무자가 아니다 보니 관련 규정을 처음부터 찾아야 했고, 규정 자체가 FDA·ICH·식약처 등 여러 사이트에 흩어져 있으며 주기적으로 개정되는데, 그 변화를 추적하는 일이 항상 수작업이었다.

## 시나리오

- **본인 사용:** 신규 필드의 검증 로직을 짤 때, 관련 규정을 자연어로 질문하면 근거 조항과 출처를 즉시 확인한다.
- **외부 실사용자 (CRA/RA):** 사이트 모니터링 중 특정 규정(예: 이탈 보고 기한)이 헷갈릴 때, 커뮤니티에서 발견한 이 도구로 빠르게 확인한다.

## 의사결정 근거 (Action)

- **왜 벡터DB를 안 썼는가:** 청크 수가 수백 개 수준인 현재 규모에서 브루트포스 코사인 유사도 계산과 ANN(pgvector HNSW와 같은 복잡도 계열)을 실측했다. 1536차원 기준 브루트포스는 100개 0.23ms, 1,000개 2.12ms, 10,000개 29.08ms, 100,000개 237.27ms로 500ms 임계값을 넘지 않았다. 청크 5,000개 또는 유사도 계산 500ms를 넘으면 전환한다는 조건을 코드와 README에 명시했다.
- **왜 개정 자동 감지를 핵심으로 뒀는가:** 검색 자체보다 "규정이 바뀌었는지 사람이 매번 확인해야 하는 문제"가 더 근본적인 비효율이라 판단해 파이프라인의 중심으로 설계했다. 1차 신호는 고시일/발행일, 2차 신호는 파일 SHA-256이며, 바뀐 조항만 재임베딩한다.
- **왜 Claude Code Stop 훅으로 자동 코드 리뷰를 넣었는가:** 에이전트가 구현한 코드를 사람이 매번 검토하는 대신, 다른 모델(Gemini/GPT)이 `git diff` 를 구조적으로 검증하게 만들어 개발 프로세스 자체의 신뢰도를 높였다.

## 측정 가능한 결과 (Result)

실사용 2–3주 로그를 쌓은 뒤 아래를 채운다.

| 지표 | 값 | 메모 |
| --- | --- | --- |
| 검색 쿼리 수 | _배포 후 기입_ | `search_logs` |
| 재방문율 | _배포 후 기입_ | IP 해시 기준 |
| 도움됨(👍) 비율 | _배포 후 기입_ | `feedback` |
| 개정 감지 지연 | Cron 주기 1일 | Vercel Cron 03:00 UTC |
| 실제 감지 개정 건수 | _운영 후 기입_ | `change_log` |
| 규정 확인 소요시간 | _본인 실측 후 기입_ | 기존 검색엔진 대비 |
| 브루트포스 vs ANN | 100개 0.23ms / 1k 2.12ms / 10k 29ms / 100k 237ms | `BENCHMARK.md` |

## 기술 스택

Next.js (App Router, TypeScript strict) · Vercel Cron · Supabase Postgres (벡터 컬럼 없음) · OpenAI `text-embedding-3-small` · Claude Haiku · Vitest · Playwright · Claude Code Stop 훅
